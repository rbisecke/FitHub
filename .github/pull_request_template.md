## Summary

<!-- What does this PR do? One paragraph. -->

## Changes

## <!-- Bullet list of key changes -->

## Test plan

- [ ] `uv run --project apps/api pytest /abs/path/to/apps/api/tests/` passes
- [ ] `supabase --workdir . test db` passes (if schema changed)
- [ ] `pnpm -C apps/web typecheck` passes (if frontend changed)
- [ ] `pnpm -C apps/web lint` passes (if frontend changed)
- [ ] E2E tests pass: `pnpm --filter web exec playwright test` (if UI changed)
- [ ] Screenshots attached below (if rendering changed)

## Notes

<!-- Anything a reviewer needs to know: gotchas, deferred items, trade-offs, failed approaches -->
