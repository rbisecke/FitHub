import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

export default function AdminPage() {
  return (
    <PlaceholderScreen
      title="Admin"
      subtitle="Operator console — access requests, user management, infra health (Domain 08). Built in Effort 10."
    />
  );
}
