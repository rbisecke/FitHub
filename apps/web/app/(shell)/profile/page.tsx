import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

export default function ProfilePage() {
  return (
    <PlaceholderScreen
      theme="light"
      title="Profile"
      subtitle="Profile and settings (Domain 08). Light — settings review (00 Part 2). Built in Effort 2."
    />
  );
}
