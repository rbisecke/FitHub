import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

export default function IntegrationsPage() {
  return (
    <PlaceholderScreen
      theme="light"
      title="Integrations"
      subtitle="Connected sources and notification feed (Domain 07). Light — seated/transactional (00 Part 2). Built in Effort 9."
    />
  );
}
