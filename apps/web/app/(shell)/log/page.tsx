import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

export default function LogPage() {
  return (
    <PlaceholderScreen
      theme="light"
      title="Log"
      subtitle="Active logging, movement catalog, and history (Domain 01). Light — the gym-legibility hard exception (00 Part 2). Built in Effort 3."
    />
  );
}
