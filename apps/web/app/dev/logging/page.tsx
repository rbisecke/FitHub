import { LoggingPreviewClient } from "./preview-client";

/**
 * Scaffold route — Effort 3 set-table variant comparison (01 §2.7). Renders both
 * variants with example data, unauthenticated, for side-by-side screenshotting.
 * Not part of the shipping app.
 */
export default function LoggingPreviewPage() {
  return <LoggingPreviewClient />;
}
