import { InjuriesPickerPreviewClient } from "./preview-client";

/**
 * Scaffold route — Effort 4 body-region-picker variant comparison (05 §1.1).
 * Renders both variants with example data, unauthenticated, for side-by-side
 * screenshotting. Not part of the shipping app.
 */
export default function InjuriesPickerPreviewPage() {
  return <InjuriesPickerPreviewClient />;
}
