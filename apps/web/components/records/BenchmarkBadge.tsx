export function BenchmarkBadge() {
  return (
    <span
      className="inline-flex items-center rounded px-1.5 py-0.5 font-mono text-[10px] uppercase font-semibold"
      style={{ background: "#30363d", color: "#d29922" }}
    >
      BENCHMARK
    </span>
  );
}
