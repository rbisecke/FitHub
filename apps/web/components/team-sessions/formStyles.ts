/** Shared light-form input styling (06 §2/§4a) — used by `TeamSessionForm`
 * and `ParticipantPicker`, both forced-light sheets. */
export const inputStyle = {
  background: "var(--bg)",
  border: "1px solid var(--border)",
  color: "var(--text)",
} as const;
