[build]
builder = "nixpacks"
buildCommand = "sh apps/api/.nixpacks/build.sh"

[deploy]
startCommand = "uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "api"
source = "apps/api"
